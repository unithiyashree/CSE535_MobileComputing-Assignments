Mandatory tasks to run:

1) Run convert_to_csv.py

	Note: Before running this python file, check whether there are .cv files under the json file folder 
	( ../Thursday_Group_16_Project2/src/jsonFiles). If there are any .csv files, delete those files before running the  convert_to_csv.py. ( This should look like the image Before_Running_Convert_to_CSV_File.PNG )
	Output looks like the image in the folder ( After_Running_Convert_to_CSV_File.PNG)

2) To train the models, run TrainModels.py 

3) To test the models, run APK.py 